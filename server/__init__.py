"""Root server package required by OpenEnv repo validation.

The canonical server implementation lives under `openenv_submission.server`.
This package exists so the validator can find `server/app.py` at repo root.
"""
